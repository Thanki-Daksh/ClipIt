[pytest]
testpaths = tests
python_files = test_*.py
filterwarnings =
    ignore::pytest.PytestUnknownMarkWarning
markers =
    ffprobe: requires ffmpeg + ffprobe on PATH (media integration tests)
    media: FFmpeg media probing tests (black-frame / silence)
    e2e: full-suite end-to-end integration tests