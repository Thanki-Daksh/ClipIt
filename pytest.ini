[pytest]
testpaths = tests
python_files = test_*.py
addopts = -m "not e2e"
markers =
    ffprobe: requires ffmpeg + ffprobe on PATH (media integration tests)
    e2e: slow end-to-end tests (excluded by default)